#include <stdio.h>


/**
 * main - it contains the command line argument
 * @argc: the number of args
 * @argv: the array of args
 * Return: 0
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
